{
  "extends": ["eslint:recommended", "prettier"],
  "rules": {
    "indent": ["error", 2], // Ensures 2 spaces for indentation
    "quotes": ["error", "single"], // Enforces single quotes
    "semi": ["error", "always"] // Requires semicolons
  }
}
